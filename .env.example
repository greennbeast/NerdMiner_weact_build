# Copy this file to .env and adjust values
# The proxy server listens on PORT
PORT=8080

# Your ESP32 miner IP address
MINER_IP=192.168.0.185

# Optional: enable HTTP Basic Auth for dashboard and API
# Set both to enable; leave blank to disable auth
DASH_USER=admin
DASH_PASS=change-me-strong-password
